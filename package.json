{
  "name": "websockets-chat-room",
  "version": "1.0.0",
  "description": "To run this application, first open up a terminal. Then, run the command below:",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  }
}
